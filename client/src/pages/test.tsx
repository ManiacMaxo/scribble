import React from 'react'
import { Canvas, Layout } from '@/components'

const test = (): JSX.Element => {
    return (
        <Layout>
            <Canvas />
        </Layout>
    )
}

export default test
